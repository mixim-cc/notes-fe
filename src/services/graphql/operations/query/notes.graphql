query getNoteFolderStructure {
  note {
    listAll{
      id
      title
      type
      children {
        id
        title
        type
      }
    }
  }
}

query getNote($id: String!) {
  note {
    get(id: $id){
      id
      title
      data
    }
  }
}
