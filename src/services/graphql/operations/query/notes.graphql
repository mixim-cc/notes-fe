query getNoteFolderStructure($search: String) {
  note {
    listAll(search: $search){
      id
      title
      type
      children {
        id
        title
        type
      }
    }
  }
}

query getNote($id: String!) {
  note {
    get(id: $id){
      id
      title
      data
    }
  }
}
